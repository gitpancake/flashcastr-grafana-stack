:3101 {
    handle /ready {
        respond "OK" 200
    }
    handle {
        basic_auth {
            {$LOKI_BASIC_AUTH_USER} {$LOKI_BASIC_AUTH_HASH}
        }
        reverse_proxy {$LOKI_UPSTREAM:loki:3100}
    }
}
